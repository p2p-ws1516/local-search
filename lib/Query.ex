defmodule Query do
	
	##
	# if from is a PID id, we assume that our client issued the query
	# otherwise we consider it a remote query
	##
	def handle(from, latlon, links, query, req_options) do
		
	end

end